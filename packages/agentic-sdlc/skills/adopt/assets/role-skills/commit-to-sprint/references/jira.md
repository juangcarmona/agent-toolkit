# commit-to-sprint: Jira

Sprint commitment is often a single workflow transition whose screen requires the team, the sprint and the estimate together. Resolve during adoption and record in the configuration:

- the transition name and id;
- the team, sprint and estimate field ids.

Team and sprint are deterministic: the board's team, and its single **open** sprint, resolved through tooling at commit time. Only the estimate needs a human.
