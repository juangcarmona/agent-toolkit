# inspect-pull-request: Azure DevOps

Votes are numeric: approved, approved with suggestions, no vote, waiting for the author, rejected. Report the vote and the reviewer, not a summary.

**Votes reset when a new commit is pushed.** An approval read before a push no longer exists after it. This is the most common reason a merge that "should" work does not.
