# search-kb: hosted wiki

Most wikis expose both a text search and a structured query language. Prefer the structured form when scoping to a space or a label; text search alone returns the whole instance.

Record during adoption: the space key or equivalent scope, and whether the session's credentials can read it. A search returning nothing because of a permission boundary looks identical to a search returning nothing because nothing matched: distinguish them.
