# update-pull-request: Azure DevOps

Clearing the draft flag marks the pull request ready and starts required policy evaluation. Until that moment, validation has not run, so a draft with no failing checks is not a passing pull request.

Adding a reviewer notifies them. Add only reviewers the project's configuration names.
