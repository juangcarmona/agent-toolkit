# classify-doc-drift: Markdown in the repository

Where documentation lives only in the repository there is no pair, and this role is not installed. Adoption should say so rather than installing a role that always returns "unmapped".
