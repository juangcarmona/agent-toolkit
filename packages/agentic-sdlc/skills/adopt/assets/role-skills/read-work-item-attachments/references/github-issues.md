# read-work-item-attachments: GitHub Issues

Attachments are links embedded in the Markdown body, not a structured field. Extract image and file URLs from the body, and treat anything on the user content host as an attachment.

A private repository's attachment URLs require authentication to fetch.
