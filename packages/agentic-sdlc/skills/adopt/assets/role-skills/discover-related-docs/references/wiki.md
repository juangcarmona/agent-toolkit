# discover-related-docs: hosted wiki

Derive search terms from the item's own vocabulary (its title, its components, the systems named in its description), rather than from generic domain words. Search each, then de-duplicate by page.

Where the wiki uses labels, a label search is far more precise than text. Record the labels the project actually uses during adoption.
