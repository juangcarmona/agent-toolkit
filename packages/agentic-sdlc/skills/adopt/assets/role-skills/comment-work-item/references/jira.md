# comment-work-item: Jira

Comment bodies use the same document structure as other rich-text fields. Build the structure; do not paste Markdown and expect it to render.

Beware typed-text autoformatting when a comment is entered through a browser rather than an API: sequences like `8)` and `(c)` are silently replaced with an emoji and a symbol.
