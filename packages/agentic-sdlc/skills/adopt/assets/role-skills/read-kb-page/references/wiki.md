# read-kb-page: hosted wiki

Request the storage or structured representation rather than the rendered view. Rendered HTML loses macro content and can silently drop embedded material.

Where a page embeds diagrams or attachments, note their presence even when their content cannot be extracted. A page that reads as complete but is missing its diagram misleads every reader downstream.
